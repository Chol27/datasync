cd database
echo -n "POSTGRES_PORT="
read POSTGRES_PORT
echo "Your POSTGRES_PORT is $POSTGRES_PORT"
echo -n "POSTGRES_USERNAME="
read POSTGRES_USERNAME
echo "Your POSTGRES_USERNAME is $POSTGRES_USERNAME"
echo -n "POSTGRES_PASSWORD="
read POSTGRES_PASSWORD
echo "Your POSTGRES_PASSWORD is $POSTGRES_PASSWORD"
echo -n "POSTGRES_DATABASE="
read POSTGRES_DATABASE
echo "Your POSTGRES_DATABASE is $POSTGRES_DATABASE" 
echo "POSTGRES_PORT=$POSTGRES_PORT
POSTGRES_USERNAME=$POSTGRES_USERNAME
POSTGRES_PASSWORD=$POSTGRES_PASSWORD
POSTGRES_DATABASE=$POSTGRES_DATABASE" > .env
source ./script/.init
source ./script/.set-index

cd ../client
sqlite3 appdata.db < sqlitedump.sql

cd ../server
echo -n "SERVER_PORT="
read SERVER_PORT
echo "POSTGRES_PORT=$POSTGRES_PORT
POSTGRES_USERNAME=$POSTGRES_USERNAME
POSTGRES_PASSWORD=$POSTGRES_PASSWORD
POSTGRES_DATABASE=$POSTGRES_DATABASE

SERVER_PORT=$SERVER_PORT" > .env
npm install

npm run start:dev
